<?php

require_once (dirname(__FILE__)."/../src/Anagrams.php");

class AnagramsTest extends PHPUnit_Framework_TestCase {

	public function test_sample(
	) {
		$this->fail("Ouch!");
		/* OTHER USEFUL ASSERTS:
			$this->assertEquals($expectedValue, $whatever);
			$this->assertNotEquals($notExpectedValue, $whatever);
			$this->assertTrue($whatever);
			$this->assertFalse($whatever);
		*/
	}

}
